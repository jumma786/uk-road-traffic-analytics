services:
  - type: web
    name: uk-road-traffic-dashboard
    runtime: python
    buildCommand: pip install -r requirements-cloud.txt
    startCommand: streamlit run src/dashboard/app_cloud.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
